<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Spectre.Views.QueueControl" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:views="clr-namespace:Spectre.Views;assembly=Spectre">
  <Border Name="QueueSidebarBorder" Width="350" HorizontalAlignment="Left" Background="{DynamicResource SidebarBrush}" Visibility="Collapsed">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <TextBlock Text="Queue" Foreground="{DynamicResource TextBrush}" FontSize="18" FontWeight="Bold" Margin="20,20,20,10" Grid.Row="0" />
      <ListBox Name="QueueSidebarPanel" Grid.Row="1" Margin="10,0,10,20" Background="#00FFFFFF" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.ScrollUnit="Pixel" ItemsSource="{Binding Items}" PreviewMouseWheel="QueueSidebarPanel_PreviewMouseWheel">
        <ItemsControl.ItemContainerStyle>
          <Style TargetType="{x:Type ListBoxItem}">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="#00FFFFFF" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                  <ContentPresenter />
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Name="ItemBorder" CornerRadius="6" Padding="6,5,6,5" Margin="0,2,0,2" Cursor="Hand">
              <Border.Background>
                <SolidColorBrush Color="Transparent" />
              </Border.Background>
              <UIElement.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding PlayCommand}" />
              </UIElement.InputBindings>
              <Grid>
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="24" />
                  <ColumnDefinition Width="40" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12" Text="{Binding NumberText}">
                  <FrameworkElement.Style>
                    <Style TargetType="{x:Type TextBlock}">
                      <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
                      <Style.Triggers>
                        <DataTrigger Value="True" Binding="{Binding IsPlaying}">
                          <Setter Property="Foreground" Value="{DynamicResource AccentGradient}" />
                        </DataTrigger>
                      </Style.Triggers>
                    </Style>
                  </FrameworkElement.Style>
                </TextBlock>
                <Border Grid.Column="1" Width="32" Height="32" CornerRadius="4" Background="#1E1E28" ClipToBounds="True">
                  <Image Stretch="UniformToFill" Source="{Binding ThumbnailUrl, NotifyOnTargetUpdated=True}" TargetUpdated="Image_TargetUpdated" />
                </Border>
                <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="10,0,0,0">
                  <TextBlock FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Text="{Binding Title}">
                    <FrameworkElement.Style>
                      <Style TargetType="{x:Type TextBlock}">
                        <Setter Property="Foreground" Value="#FFFFFFFF" />
                        <Style.Triggers>
                          <DataTrigger Value="True" Binding="{Binding IsPlaying}">
                            <Setter Property="Foreground" Value="{DynamicResource AccentGradient}" />
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </FrameworkElement.Style>
                  </TextBlock>
                  <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11" TextTrimming="CharacterEllipsis" Text="{Binding Artist}" />
                </StackPanel>
              </Grid>
            </Border>
            <DataTemplate.Triggers>
              <Trigger Property="UIElement.IsMouseOver" Value="True">
                <TriggerBase.EnterActions>
                  <BeginStoryboard>
                    <Storyboard>
                      <ColorAnimation To="#14FFFFFF" Duration="0:0:0.1" Storyboard.TargetName="ItemBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" />
                    </Storyboard>
                  </BeginStoryboard>
                </TriggerBase.EnterActions>
                <TriggerBase.ExitActions>
                  <BeginStoryboard>
                    <Storyboard>
                      <ColorAnimation To="Transparent" Duration="0:0:0.2" Storyboard.TargetName="ItemBorder" Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)" />
                    </Storyboard>
                  </BeginStoryboard>
                </TriggerBase.ExitActions>
              </Trigger>
            </DataTemplate.Triggers>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ListBox>
    </Grid>
  </Border>
</UserControl>
